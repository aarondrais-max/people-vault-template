# Claude Code entry point

All agent instructions for this vault live in **[AGENTS.md](AGENTS.md)**. Read that file.

This pointer exists only so Claude Code finds the instructions by its own convention. It is deliberately not a copy — `AGENTS.md` is the single source of truth, so the two can never drift. If you move to a different agent, point that agent at `AGENTS.md` and delete this file.
